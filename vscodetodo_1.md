# Portfolio 網站 — 待在 VS Code 處理的項目

Figma 檔案:https://www.figma.com/design/2ZgWK6lIbYXUo7F5Bjuv8o/portfolio

此文件用來記錄「Figma 裡沒辦法/不適合做,需要留到寫 code(VS Code)時一起處理」的項目。之後有新的類似狀況,直接往下加一個章節即可,等全部整理完再一次改。

**怎麼讀這份文件**:每個章節開頭都有一行 `**狀態:**`,**先看那一行**。已完成的項目底下的敘述保留原樣(當作決策紀錄),但當時的假設可能已經被推翻了——狀態行會註明。標記的意思:

- ✅ 已完成
- ⬜ 待處理

待辦 3 ~ 7 是最早整理的那批(全部已完成),待辦 8 ~ 13 是 2026-08-18 補上的,大多是**要屋主提供內容或拍板**、不是程式端的工作。

**所有待辦都已結案(3 ~ 15)。程式端沒有待辦了。**

剩下的唯一一件事不是寫程式:**推上 GitHub**(屋主已決定要 public),還沒有 remote。

各頁面的 frame id、技術棧、程式結構請看 `PROJECT_CONTEXT.md`,這份不重複。

---

## 待辦 3:(RWD)——Figma 維持 1440,實際適配留給 code

**狀態:✅ 已完成(2026-08-18)。下面「決定」那段的前提已經作廢——後來 Figma 補了 1920 / 768 / 390 三套稿,不是只有 1440。實際做法見本節末尾的「最後怎麼做的」。**

### 決定

Figma 設計稿維持用 **1440px 寬**當作電腦版的參考基準(不改成 1920),原因:
- 1440 是常見的設計基準寬度(Figma 預設桌面尺寸、也接近許多筆電的邏輯解析度)。
- 不管設計稿用 1440 還是 1920,都無法涵蓋所有使用者的實際螢幕寬度,響應式適配本來就得留到 code 端處理,所以沒必要為了「多涵蓋一點螢幕」就把整份已經用絕對定位做好的稿子重改成 1920(改稿工作量很大,詳見下方)。

### 需要在 VS Code / code 端做的事

1. **建立響應式斷點(breakpoints)。** 目前查到的實際桌面解析度分佈大致是:1920×1080(~22%)最主流,其次 1536×864(Windows 125% 縮放後的 1920 螢幕)、1366×768、1280×720 / 1280×1200 這幾個較窄的機型合計也有相當比例。至少要處理:
   - **≥1440px(含 1920 以上大螢幕)**:內容區保持最大寬度(建議 1200~1440px)置中,背景/大圖可以延伸滿版到螢幕邊緣,避免螢幕越寬、兩側空白越大或內容被過度拉伸。
   - **1280~1440px(常見窄螢幕筆電)**:文字排版、四個分類長條、Resume 三欄技能區塊等,可能需要縮小字級或調整欄寬,避免超出可視範圍。
   - **平板 / 手機**:目前 Figma 完全沒有手機版設計,如果網站需要支援手機瀏覽,這部分要嘛之後另外設計手機版 Figma 稿,要嘛直接在 code 端用響應式規則(例如單欄堆疊)重新編排,需要先決定要不要另外出手機版設計稿。
2. **目前 Figma 裡的排版都是絕對定位(x/y 座標),不是用百分比或 auto-layout。** 轉成 code 時不能直接照抄座標,需要重新用 flexbox / grid 等方式做出「會跟著容器寬度變化」的排版邏輯,這是 design-to-code 階段本來就要做的事,不是單純套用 Figma 座標就好。
3. 呼應待辦 1(四條長條貼合 Vector 47 曲線 + hover 熱區):曲線裁切在不同螢幕寬度下的表現(clip-path 的座標可能要用相對單位或用 JS 動態計算)也要一併考慮進響應式邏輯裡,不能只按 1440 寬寫死。

### 最後怎麼做的

上面第 1、2 點的假設後來都變了,實際情況記錄如下:

- **四套稿,不是一套。** Figma 最後畫了 390 / 768 / 1440 / 1920 四組完整的頁面。而且它們**不是同一個版面的不同尺寸,是各自手排的**——例如 `image 92` 在 1440 稿位於 (585, 1123),到 1920 稿搬到 (1507, 177);Artwork 整頁高度 1440 是 9435、1920 是 8582。所以沒有「用 flexbox 重新編排一次就好」這回事,每個斷點都得有自己的一套座標。
- **斷點就是設計寬度本身**:`≤767.98` 用 390 稿、`768–1199` 用 768 稿、`1200–1727` 用 1440 稿、`≥1728` 用 1920 稿。
- **縮放方式**:每個斷點用自己的稿,靠 root font-size 等比縮放(`font-size: calc(100vw / 設計寬 * 16)`),所以絕對座標可以照抄,寫成 `rem` 就會自己跟著螢幕縮放,不用逐一改成百分比。低於設計寬度自由縮小;高於設計寬度只放大一點點(390 上限 1.25×、768 上限 1.15×、1440 和 1920 不放大),超過就固定置中,滿版元素(波浪、footer)拉到螢幕邊緣。
- **Artwork 頁的 49 個圖塊改成資料驅動**(`lib/artwork.ts`),每個圖塊帶四套座標當 CSS 變數,整份 CSS 只用四條 media query 切換要讀哪一套。以後要加斷點只需補資料,不用動版面。
- 一個已知限制:root font 用 `100vw` 算,而 `100vw` 含捲軸寬度。桌機把視窗縮到 768 以下時構圖會比可用寬度多約 15px(`overflow-x: clip` 擋住了,內容會左移一點)。手機是覆蓋式捲軸不受影響。

---

## 待辦 4:Artwork 頁——影片素材 + 分類結構

**狀態:✅ 全部完成(2026-08-18)。第 1 點採 YouTube 嵌入(方案 B),10 支影片都接好了;第 2 點早已完成;第 3 點已獨立成待辦 8。做法見本節末尾的「最後怎麼做的」。**

> 補充(2026-08-18):匯出到 `public/assets/images/artwork/` 的那 10 張動畫縮圖現在全是黑底。其中 `01_katana_show_video.png`、`02_MV_yeren.png`、`05_FAD_logo_animation1.png` 這三個檔案**位元組完全相同**——因為它們在 Figma 裡都只是空的黑色佔位框,匯出結果自然一樣。換上真正的影片(或各自的封面圖)之後才會有差別。
>
> **上面這段講得太誇張了,實際量過像素只有 4 張是黑的**:`01_katana`、`02_MV_yeren`、`03_FAD_logo_animation2`、`05_FAD_logo_animation1`。其餘 6 張(`04_stop_motion`、`06_FAD_logo_animation3`、`07`、`08`、`09`、`31_3d_animation_story`)都有正常畫面。

Figma 內對應 frame:`Artwork`(node id `328:488`)

1. **影片檔案太大,Figma 裡放不了,只能看到黑色縮圖。** 目前確認至少這幾個是影片、不是圖片壞掉:`3D produck assigment 2 1/2/3`(node `350:568`/`350:569`/`350:571`)、`week 13 final project_2 1`(`350:573`)、`Comp 2 1`(`350:574`)、`FAD logo 1 1` / `FAD logo 2 1`(`350:572`/`350:570`,待確認是不是影片)。這些要留到 VS Code:存放實際影片檔(或上傳到 YouTube/Vimeo 之類的地方用 embed),並設計靜態封面縮圖(現在是純黑色,體驗不好)。
2. **Artwork 頁分類/加標題 —— ✅ 已在 Figma 裡完成。** 已經沿用 Home 頁的四個分類名稱(Visual Product / 2D & 3D Animation / Studio Drawing / Poster Design),把原本混在一起的作品依內容(不是只看檔名)拆分排列,每區加了標題+簡介文字,區塊間用細分隔線隔開。使用者後續有自己手動微調過排版(照片大小、位置、每區留白節奏),目前是最終手動排版版本,不是 auto-layout 網格。「點分類篩選只顯示該類作品」如果要做,篩選邏輯留到 code 端(詳見下面待辦 5)。
3. 目前作品都沒有標題/簡短說明(用什麼工具做的、課堂作業還是自主創作),建議之後補上,方便訪客理解脈絡。

### 最後怎麼做的

選了**方案 B:影片放 YouTube,網站用嵌入**,不自架 mp4。

**版面上的分工**:Artwork 列表頁那 10 個格位**還是放原本的靜態縮圖**,不是把播放器塞進去——那一頁是一整張手排的畫布,格位大小形狀都不一樣,塞 10 個播放器會把構圖拆掉,而且一次載 10 個 YouTube player 會很慢。**影片是放在詳情頁**(`/artwork/an-01` ~ `an-10`),點縮圖進去看。

**10 支影片對照表**(填在 `lib/artwork.ts` 每筆的 `youtubeId`):

| slug | 縮圖檔 | 影片 | YouTube id |
|---|---|---|---|
| `an-01` | `05_FAD_logo_animation1.png` | FAD logo animation 1 | `PD2BqAp6gFY` |
| `an-02` | `03_FAD_logo_animation2.png` | FAD logo animation 2 | `v_ij-gYtqHo` |
| `an-03` | `06_FAD_logo_animation3.png` | FAD logo animation 3 | `FfveeIXrr1M` |
| `an-04` | `02_MV_yeren.png` | MV 野人 | `Of31HehQg1I` |
| `an-05` | `04_stop_motion.png` | Stop motion | `gwD1F2Vcamk` |
| `an-06` | `01_katana_show_video.png` | Katana showcase | `bEGi2ncD3Jg` |
| `an-07` | `07_2d_front_walking_cycle.png` | 2D front walking cycle | `yNuLK-KMNM8` |
| `an-08` | `08_2d_talking_animation.png` | 2D talking animation | `CySX0Ps2Pac` |
| `an-09` | `09_2d_exam_show_animation.png` | 2D exam show animation | `1ppnhdq5-aQ` |
| `an-10` | `31_3d_animation_story.png` | 3D animation story | `ciWffF7ccwY` |

幾個實作上的決定:

- **有影片就能點進去,不用等文案。** 原本的規則是「有寫標題才開詳情頁」(待辦 6),但影片本身就是作品,為了一段還沒寫的敘述把 10 支片子鎖住不合理。所以規則改成**「有文案 or 有影片」**。標題暫時用 `alt`(例如「Katana showcase video」),等待辦 8 寫了真標題就會自動換掉。純圖片的作品規則沒變,還是要有標題才開。
- **縮圖上加了播放標記**(灰底圓形 + 白色三角形)。沒有它的話,那 4 張純黑的縮圖看起來就只是壞掉的圖。標記大小是用 `cqw` 跟著格位縮放的,不是固定 px——同一個標記要同時待在 390 的 138px 格位跟 1440 的 569px 格位上。滑到有文案的作品上時標記會淡出,把版面讓給 hover 疊層。
- **用 `youtube-nocookie.com`**,而且沒有 autoplay。訪客沒按播放之前 YouTube 不會寫任何東西,省掉 cookie 同意條的問題。
- **播放器用 16:9 塞進設計稿的圖框裡**(1440 是 680×452,不是 16:9),寬高哪個先滿就用哪個,置中。直接讓 iframe 撐滿圖框的話,YouTube 會自己補黑邊,在這個白底頁面上很難看。
- 詳情頁的**上一件/下一件箭頭現在會出現了**——已發佈的作品從 1 件變成 11 件,終於有前後可以去。

**✅ 已驗證(2026-08-18 稍晚)**:原本這裡寫「開發環境連不到 youtube.com,沒看到影片播放」——後來連得上了,實測 `/artwork/an-05` 播放器正常載入(縮圖、頻道名「Gaifoon Chai」、播放鍵都出來了)。另外用 YouTube oEmbed API 逐支查過 10 個影片 id,**全部回 200**,代表都存在且不是「私人」,嵌入不會出問題。

**如果影片顯示「無法播放」**:多半是那支片在 YouTube 上設成了**私人**,或是關掉了嵌入權限。要能嵌入,影片必須是「公開」或「不公開(未列出)」,不能是「私人」。

---

## 待辦 5:Artwork 頁分類導覽 —— 捲動自動高亮(scroll-spy)+ hover/pressed 樣式規格

**狀態:✅ 已完成(2026-08-18)**

> 實作方式跟下面建議的 `IntersectionObserver` 略有不同:四個分類區塊在畫布上是首尾相接的,所以直接用「視窗往下 40% 那條線落在哪一區」來判斷,結果精確且沒有交界處的模糊地帶(用 IntersectionObserver 的話,band 要夠寬才會穩定觸發,但夠寬就會在每個接縫同時命中兩區)。Hover / Pressed 的疊加優先權照第 4 點處理。點擊時會先鎖住 700ms 不讓捲動判定覆寫,否則平滑捲動的過程會把高亮一路閃過中間每一個分類。四個斷點的 nav 位置與尺寸都在 `lib/artwork.ts` 的 `SUBNAV`。

### 背景

Artwork 頁左上角(x=70, y=161 起)有 4 行分類導覽文字,現在對應 Figma component set:

| 名稱 | Component Set id | Default variant | Hover variant | Pressed variant | 目前頁面上的 instance id | 點擊捲動目標(該分類標題文字) |
|---|---|---|---|---|---|---|
| Visual Product | `377:130` | `377:125` | `378:129` | `377:127` | `377:149` | `358:139` |
| 2D & 3D Animation | `377:136` | `377:131` | `378:133` | `377:133` | `377:153` | `369:158` |
| Studio Drawing | `377:142` | `377:137` | `378:137` | `377:139` | `377:157` | `371:168` |
| Poster Design | `377:148` | `377:143` | `378:141` | `377:145` | `377:161` | `371:169` |

三個狀態的樣式規格:

- **Default**:文字黑色(`#000000`),不位移。
- **Hover**(滑過去):文字變 `#A8A8A8`,位置不動,不出現方塊。
- **Pressed**(點下去 / 目前選中):文字 `#A8A8A8` 且往右位移 `16px`;同時出現一個 `16×2px`、顏色 `#626262` 的小方塊,方塊左邊對齊(位移後的)文字左邊,方塊上緣距離文字框頂端 `38px`(字型 Biryani Light 18px,line-height 60px)。

Figma 裡目前已經接好的原型互動(僅 Figma Present 模式可預覽,不是最終網站行為):

- `ON_HOVER` → 切到 Hover variant(滑開自動變回 Default,Figma 內建行為)。
- `ON_CLICK` → 切到 Pressed variant,**同時**觸發 `SCROLL_TO` 捲動到對應分類標題文字。

### 需求(要留到 code 端做的部分)

**捲動時要能自動判斷目前畫面在哪個分類區域,並讓對應的 nav 項目自動變成 Pressed 樣式**(scroll-spy),不是只有點擊才會變。

### 為什麼不在 Figma 裡做

Figma 的 Prototype 互動系統裡沒有「捲動位置觸發(scroll into view)」這種 trigger——現有的 trigger 只有點擊、滑過、按下、拖曳、計時器、鍵盤、媒體事件這幾種,沒有「捲動到某個區塊時觸發」的機制。所以 hover 自動變色、點擊變 Pressed+跳轉,這兩個都能在 Figma 裡預覽;但「捲動經過時自動變成選中樣式」這個純粹是網頁前端行為,Figma 原生做不到,已確認是平台限制,不是漏做。

### 在瀏覽器 / code 端的解法

1. 用 `IntersectionObserver` 監測 Poster Design / Studio Drawing / Visual Product / 2D & 3D Animation 四個分類區塊(對應 Figma 標題文字 `358:139` / `369:158` / `371:168` / `371:169` 所在的區域)是否進入可視範圍(建議 threshold 抓區塊上緣進入螢幕上半部時就切換,體驗比較好)。
2. 進入可視範圍的區塊,幫對應 nav 項目加上「pressed」的 class/style(照上面 Pressed variant 的規格:color `#A8A8A8`、`transform: translateX(16px)`,加一個 `16×2px`、`#626262` 的小方塊,方塊 `left` 對齊文字、`top` 距文字框頂端 `38px`),其餘 nav 項目退回 Default 樣式。
3. 點擊 nav 項目時維持原本行為:捲動到對應區塊 + 該項目立即變 Pressed(不用等 IntersectionObserver 反應過來)。
4. hover 效果(滑過去變 `#A8A8A8`、不位移、不出現方塊)用 CSS `:hover` 就能做,跟 scroll-spy 的 Pressed 狀態是疊加關係(hover 只影響顏色,Pressed 決定要不要位移+方塊),寫的時候要注意兩者疊加時的優先順序(建議:Pressed 狀態優先於 Hover 的位移/方塊效果,但 hover 顏色可以互相覆蓋不衝突)。

---   

## 待辦 6:Artwork 作品詳情頁(hover 淡出 + 點擊詳情)—— prev/next 待做,全部作品要用 template 生成、不手刻

**狀態:程式部分 ✅ 已完成(2026-08-18);⬜ 卡在內容——48 件作品裡只有 The Duck 寫了文案,見待辦 8。**

> 做出來的樣子跟原本的假設有一點不同,記錄如下:
> - 它是**全螢幕疊層**,不是一般頁面:沒有 header/footer,不透明底色鋪滿,右上角關閉鈕,左右箭頭。所以 `components/SiteChrome.tsx` 讓 `/artwork/[slug]` 這條路由不套用站台的 header/footer。
> - 路由是 `/artwork/[slug]`(真實路由,不是 modal),所以有網址可分享、瀏覽器上一頁正常。
> - Figma 後來補齊了 390 / 768 / 1440 / 1920 四個 frame,**四個都是 790 高**。程式把整個構圖等比縮放去貼合視窗(寬高哪個先用完就用哪個當基準),所以任何螢幕都不會出現捲軸。詳見待辦 10。
> - 文案來源是 `lib/artwork-content.ts`,**hover 疊層跟詳情頁共用同一份**:只要某件作品填了 `title`,它的 hover 效果和詳情頁就一起開啟;沒填的維持普通圖片、點不進去,不會出現半成品頁面。
> - prev/next 依「已發佈」的順序繞回。目前只有一件,所以箭頭不顯示(不做成沒作用的按鈕)。
> - 關閉鈕回到 `/artwork#region-<分類>`,會落在原本瀏覽的分類位置。

### 背景

在 Artwork 頁的 Visual Product 分類裡,以「The Duck」這張照片做了一個範例:

- Component set `show hover`(id `373:244`),兩個 variant:`Property 1=Default`(`373:243`,正常顯示照片)、`Property 1=Variant2`(`373:245`,照片淡出 + 疊加標題「The Duck」+ 一段敘述文字)。
- Instance `373:254` 放在原本 image 78 的格位(x=370, y=405, 344×364),取代了原本的靜態圖片。
- Hover 互動:instance 上設 `MOUSE_ENTER` → `CHANGE_TO` Variant2(Smart Animate,0.5s,ease-in-out);Variant2 這個變體自己身上另外設了 `MOUSE_LEAVE` → `CHANGE_TO` 回 Default——用每個變體各自管自己離開時的行為,滑開會正確復原,這個做法是對的。
- 點擊互動:instance 上設 `ON_CLICK` → `NAVIGATE` 到詳情頁 frame `Artwork inside show`(`373:210`)。
- 詳情頁內容:大圖(`373:226`)、標題「The Duck」(`373:228`)、敘述文字(`373:229`)、EXIF 風格資訊「Camera model EOS R100 / ISO speed ISO-500 / F-stop f/6.3 / Exposure time 1/400 sec」(`373:230`)、跟 Contact/Resume 頁一致的圓圈裝飾背景(`373:211`~`373:214`)、左右兩個像是上一件/下一件作品的箭頭 Vector(`373:234` 左邊、`373:238` 右邊)、右上角一個關閉/返回用的 Vector(`373:241`,已經接了 `ON_CLICK` → `NAVIGATE` 回 Artwork 頁 `328:488`,這條動線是通的)。

### 需求

1. **左右箭頭(`373:234` / `373:238`)目前沒有接任何 reaction。** 這不是漏掉,是這個「上一件/下一件作品」的切換功能本來就還沒開始做,之後要補。
2. **不要在 Figma 裡把 57 件作品都各自複製一個像 `373:210` 這樣的詳情 frame。** 這個「The Duck」範例做到滿意就好,其餘作品全部改成用同一份 code template(標題/敘述/類別/meta 欄位/圖片路徑從資料檔讀出來)生成詳情頁,不在 Figma 裡一個個手刻——這點跟使用者的想法一致,已確認方向。

### 為什麼不在 Figma 裡做(全部作品的量)

- 57 件作品如果每件都手動複製一個詳情 frame + 一個 hover component instance,量很大,而且之後要調版型(標題字級、留白、meta 欄位排版)得改 57 次,維護成本太高。
- 「上一件/下一件」如果要在 Figma 裡對 57 件作品各自接對應的 NAVIGATE 目標,等於要手動連 57×2 條線,不實際。
- 這件事的本質是「同一個版型套不同資料」,是資料驅動(data-driven)的網頁邏輯,本來就該用 code 端的 template 系統做(例如一個 React 元件 + 一份作品清單 JSON/CMS),不是 Figma 適合處理的規模。

### 在瀏覽器 / code 端的解法

1. 把 `373:210` 的版面(大圖 + 標題 + 敘述 + meta 資訊 + 裝飾圓圈背景 + 左右箭頭 + 關閉鍵)刻成一個可重複使用的 detail template component/route(例如 `/artwork/[slug]`)。
2. 建一份作品資料清單(JSON 或 Markdown frontmatter 皆可),每件作品至少要有:標題、敘述、所屬分類(Visual Product / 2D & 3D Animation / Studio Drawing / Poster Design)、圖片(或影片,呼應待辦 4 第 1 點)路徑,以及**依分類而不同的 meta 欄位**:
   - Visual Product 底下屬於「攝影」的作品:沿用 `373:230` 這種 EXIF 格式(Camera model / ISO / F-stop / Exposure time)。
   - Visual Product 底下屬於「3D 渲染」,以及 Poster Design / Studio Drawing / 2D & 3D Animation:EXIF 欄位不適用,需要換成別的欄位,例如使用的軟體(Photoshop / Blender / Maya…)、是課堂作業還是自主創作(呼應待辦 4 第 3 點)。
3. Hover 淡出 + 標題敘述的效果,直接照 `show hover` component 兩個 variant 的樣子用 CSS `:hover`(或 mouseenter / mouseleave)重做,邏輯上跟現在 Figma 的 Smart Animate 行為一致。
4. 上一件 / 下一件箭頭:根據作品清單的順序(或依分類分別排序)算出前一筆 / 後一筆的 slug,點擊換頁或用 client-side router 切換,不用像 Figma 那樣手動指定固定目標。
5. 關閉 / 返回鍵沿用 `373:241` 現在的邏輯(回到 Artwork 列表頁),可以額外記住列表當時捲動到哪個分類、返回時捲回原位置(比較細緻的體驗優化,非必要)。

### 需要的素材(已經在 Figma 裡做好的範例,連結 7 天後會過期,到時候重新用 Figma MCP 抓一次)

- Component set:`show hover`(`373:244`),variants `373:243`(Default)/ `373:245`(Variant2)
- 範例 instance:`373:254`(目前放在 Visual Product 分類、原本 image 78 的格位)
- 詳情頁範例 frame:`Artwork inside show`(`373:210`),內含大圖 `373:226`、標題 `373:228`、敘述 `373:229`、EXIF meta `373:230`、裝飾圓圈 `373:211`~`373:214`、左右箭頭 `373:234` / `373:238`(待接)、關閉鍵 `373:241`(已接,回 `328:488`)

---

## 待辦 7:全站 Header 導覽列(Home/Artwork/Resume/Contact)—— 切頁指示條「拉長回彈」彈性動畫

**狀態:✅ 已完成(2026-08-18)。做法跟下面「在瀏覽器 / code 端的解法」建議的兩段式(先拉長、再收回)不一樣,改用彈簧物理一次算完,細節見本節末尾的「最後怎麼做的」。**

> 現況:指示條已經改成用 JS 量測 DOM 位置(不是寫死 Figma 座標),所以位置會跟著實際文字寬度走,四個斷點都正確。

### 背景

Home / Artwork / Resume / Contact 這 4 個主要頁面的 header 都有一列 nav 文字(Home/Artwork/Resume/Contact),文字下方各自有一個小方塊指示條,標示目前在哪一頁,對應 Figma node:

| 頁面 | Frame id | nav 文字 x 座標(Home/Artwork/Resume/Contact) | 指示條 node id | 指示條目前 x | 指示條尺寸 | 顏色 |
|---|---|---|---|---|---|---|
| Home | `395:387` | 910 / 1028 / 1167 / 1298 | `395:400` | 910 | 11×2px | `#626262` |
| Artwork | `328:488` | 910 / 1028 / 1167 / 1297 | `328:501` | 1028 | 11×2px | `#626262` |
| Resume | `275:351` | 910 / 1028 / 1167 / 1297 | `275:364` | 1168 | 9×2px | `#626262` |
| Contact | `234:251` | 910 / 1028 / 1167 / 1297 | `262:303` | 1298 | 9×2px | `#626262` |

四個 nav 文字節點(每頁各自的 Home/Artwork/Resume/Contact 文字)已經接好 `ON_CLICK` → `NAVIGATE` reaction,跳到對應頁面的 frame,並套用 `SMART_ANIMATE` transition(0.3s,ease-in-and-out)。因為 4 個 frame 裡的指示條都同名「Rectangle 83」,Figma 的 Smart Animate 會自動抓到這個共同圖層,在切頁時把指示條從舊位置**直線平移**到新位置——這部分已經做好,可以在 Figma Present 模式看到。

**尚未做到的效果**:指示條在移動過程中要「拉長一點」(寬度變寬,像被拉伸的橡皮筋),抵達目的地後再「縮回原本的寬度」定住——類似很多網站/App 的 tab 指示條那種有彈性、Q 彈的手感,不是單純等速平移。

### 需求

1. 指示條從 A 點移動到 B 點時,寬度要跟著移動距離動態拉長(距離越遠可以拉得越長,或設一個上限),移動過程中同時橫跨/貫穿在起點跟終點之間。
2. 抵達終點後,寬度快速收縮回原本的 9~11px,位置定在終點文字下方。
3. 整體節奏要有彈性感(overshoot 一點點再回彈,或用 spring 曲線),不是等速直線移動。

### 為什麼不在 Figma 裡做

- Figma 的 `Smart Animate` 只能在「起點 frame 裡該圖層的屬性值」跟「終點 frame 裡同名圖層的屬性值」這兩個端點之間做插值。這 4 個 frame 裡指示條的寬度本來就都是同樣的 9~11px,兩個端點的寬度值相同,沒有第三個「中間變寬」的數值可以插——單一組 NAVIGATE + Smart Animate 天生做不出「中途變寬、抵達後變窄」的效果。
- 若要在 Figma 裡硬做,得另外做出「拉長狀態」的 component 變體(寬度=兩個 nav 項目之間的距離),透過「先 `CHANGE_TO` 拉長變體(同 frame 內 Smart Animate)→ 再用 `AFTER_TIMEOUT` 觸發真正的 `NAVIGATE` 跳頁」兩段式 reaction 來模擬。但 4 個 nav 項目兩兩之間的距離都不同(見下方距離表),等於要另外做 6 組不同寬度的「拉長」變體,而且原本 12 條 NAVIGATE 連結都要改成兩段式接法。更根本的問題是 Figma 的 `AFTER_TIMEOUT` 只能設固定秒數,沒辦法像 CSS transition / JS spring 那樣自動依移動距離調整動畫時長,近距離跟遠距離用同樣秒數,速度感會不一致,做出來還是「近似值」,不是真正的彈簧物理效果。已確認評估過這條路,決定不做,直接留給 code 端處理。

各 nav 項目兩兩之間的水平距離(供 code 端拉長動畫參考):

| 起訖 | 距離(px) |
|---|---|
| Home ↔ Artwork | 118 |
| Home ↔ Resume | 257 |
| Home ↔ Contact | 387~388 |
| Artwork ↔ Resume | 139 |
| Artwork ↔ Contact | 269~270 |
| Resume ↔ Contact | 129~130 |

### 在瀏覽器 / code 端的解法

1. 指示條改用 `position: absolute`(相對 nav 列容器),用 `left`(或 `transform: translateX`)+ `width` 兩個屬性一起做動畫,而不是只有位移。
2. 常見做法(「liquid / jelly tab indicator」):
   - 點擊/切換當下,先讓指示條的 `left` 對齊原位置不動、`width` 快速拉伸到「涵蓋原位置到目標位置的整個範圍」(可以用 `cubic-bezier` 的 ease-out,或彈簧函式庫如 Framer Motion / react-spring / GSAP 的 spring 曲線)。
   - 拉伸到底之後,再讓 `left` 移到目標位置、`width` 收縮回原本的 9~11px,收尾用稍微有 overshoot 的 spring easing(例如 `overshootClamping: false` 的 spring,或 CSS 的 `cubic-bezier(0.34, 1.56, 0.64, 1)` 這類「back」曲線),做出回彈感。
   - 也可以用單一 spring 同時animate `scaleX` + `translateX`(transform-origin 設在移動方向的起點),效果類似但實作更簡單,不用動態算 width。
3. 動畫時長建議依上面的距離表按比例決定(例如 `duration = base + distance * factor`),距離越遠動畫可以稍微久一點,不要用固定秒數。
4. Header 導覽的跳頁邏輯(NAVIGATE 部分)本身很單純,就是路由跳轉(Next.js `<Link>` / React Router 之類),Figma 這邊接的 `NAVIGATE` reaction 只是給 Present 模式預覽用,不需要照抄。

### 需要的素材

- 4 個 frame 的指示條 node id、響應式適配目前座標、尺寸、顏色(見上方第一個表格)
- 4 個 nav 文字節點的 x 座標(見上方第一個表格),用來算距離
- 顏色 `#626262`,跟 Artwork 頁分類子導覽 Pressed 狀態的方塊同色,风格延续一致

### 最後怎麼做的

沒有照上面第 2 點的「先拉長 → 再收回」兩段式做,而是**把指示條的左右兩個邊各自當成一顆彈簧**,兩顆的軟硬不一樣:

- 往右移動時,**右邊**(前進方向那一邊)是硬彈簧、跑得快,**左邊**是軟彈簧、慢慢跟上;往左移動就對調。
- 於是「移動中變長、抵達後縮回」是這兩顆彈簧自己跑出來的結果,不是寫死的兩個步驟——不用預先算「要拉多長」,也不用設定切換時機。
- 兩顆都調得比臨界阻尼再軟一點,所以抵達時會有一點點回彈;前進那一邊回彈得比較明顯(超過終點 2~7px,距離越遠越多),後面那一邊幾乎不晃,不然整條線看起來會抖。

實測(1440 斷點,指示條原寬 9px):

| 路徑 | 距離 | 最長拉到 | 超過終點 | 全程 |
|---|---|---|---|---|
| Home ↔ Artwork | 117px | 39px | 2.2px | 400ms |
| Resume ↔ Contact | 136px | 44px | 2.5px | 408ms |
| Home ↔ Resume | 255px | 75px | 4.7px | 417ms |
| Home ↔ Contact | 391px | 110px | 7.3px | 433ms |

拉長的幅度固定是移動距離的 **26%**,所以近距離跟遠距離的手感一致;全程時間也會隨距離微幅拉長(400 → 433ms),符合上面第 3 點想要的效果。

幾個實作上的決定:

- **只動 `transform`,不動 `left` / `width`**。指示條在版面上維持設計稿的寬度(9 / 6 / 15px,依斷點),動畫是 `translateX() scaleX()`,所以整段動畫都在合成層跑,不會每一幀重算版面。
- **座標一律現場量**(`link.offsetLeft`),不是照抄 Figma 的 x 座標,所以字型載入完、換斷點、改文字都不會跑掉。字型載入完(`document.fonts.ready`)跟視窗縮放時會**直接歸位、不做動畫**——那種時候彈一下只會像壞掉。
- **`prefers-reduced-motion: reduce`** 的使用者直接歸位,沒有動畫。
- **390 斷點沒有這條線**。手機版是漢堡選單,選中的項目左邊改成一個短標記(原本就有,沿用)。
- 分頁切到背景時瀏覽器會停掉 `requestAnimationFrame`,動畫會凍在原地、切回來再跑完——這是瀏覽器行為,不是 bug。

**檔案**:`components/Header.tsx`(彈簧常數 `LEAD` / `TRAIL` 在檔案最上面,想調手感就改這兩組)、`components/Header.module.css` 的 `.rule`。

---

## 待辦 8:48 件作品的標題 / 敘述 / meta 欄位

**狀態:✅ 已結案(2026-08-18)。標題 + 敘述 48 / 48;`meta` 16 / 48,屋主決定 meta 不需要每件都填,維持現狀。2026-08-18 由 Claude 逐張看過圖後寫了 Visual Product(22)、Studio Drawing(10)、Poster Design(5)共 37 件,加上原本的 The Duck = 38 件;同日屋主口述 10 支影片的內容,由 Claude 改寫成同一種語氣補完最後 10 件。EXIF 由屋主提供 15 張,連同 The Duck 共 16 件的 `meta` 已填。**

### meta 的現況(屋主已拍板:不用全部填)

已填 16 件,全部是攝影的 EXIF(15 張 + The Duck)。其餘 32 件維持空白:3D 渲染 7 件、Studio Drawing 10 件、Poster Design 5 件、影片 10 件。

meta 是空陣列時整條 meta 列不會渲染(`app/artwork/[slug]/page.tsx` 有 `meta.length > 0` 判斷),頁面不會壞,只是少一列資訊。

> ⚠️ **要補 meta 的話,先看這一條。** 敘述框底下就是 meta 列,**有沒有 meta 會改變敘述的可用高度**:
>
> | | 敘述可用高度(1440) |
> |---|---|
> | 沒有 meta | 155px(到 frame 底部) |
> | 有 meta | 106px(到 meta 列) |
>
> 目前有兩件敘述特別長,**只有在沒有 meta 的前提下才放得下**:
>
> | slug | 標題 | 字數 | 補了 meta 會在哪溢出 |
> |---|---|---|---|
> | `sd-05` | Portrait of Tesla | 224 | 390 與 768 |
> | `sd-02` | The Windmill | 201 | 390 |
>
> 要單獨幫這兩件補 meta,就得先把敘述壓到 **180 字元**以內。其餘 46 件補 meta 都安全。

### 為什麼原本卡

待辦 6 的 template 是資料驅動的——沒有文案就生不出東西。現在 38 件都有標題了,所以這 38 件的 hover 疊層與詳情頁都已開啟,上一件 / 下一件箭頭也正常運作。

### 文案的長度限制(寫新的時候要注意)

詳情頁是固定高度的絕對定位版面,敘述文字底下就是 meta 列,**寫太長會撞到**。已量測過的安全線:

| 斷點 | 敘述框 | 到 meta 列的距離 | 目前最高 |
|---|---|---|---|
| 390 | 278px / 12px | 144px | 108px |
| 768 | 454px / 12px | 102px | 65px |
| 1440 | 610px / 15px | 106px | 79px |
| 1920 | 610px / 15px | 106px | 79px |

現有 38 件全部**剛好等於 The Duck 的高度**,四個斷點都留有餘裕。實務上把敘述控制在 **158 字元以內**就會落在這個範圍(超過約 158 字元,390 斷點會多一行)。

### 怎麼寫

**檔案**:`portfolio/lib/artwork-content.ts`

**對照工具**:`http://localhost:3000/preview/artwork` —— 這是一個開發用的對照頁,把 49 張縮圖並排顯示,每張圖下面就是它對應的 key。已經寫好的卡片會標成綠色,最上面有「N of 48 written」的進度。沒有這個頁面的話,`vp-17` 這種 key 根本認不出是哪張圖。(這頁沒有任何連結指向它,之後可以直接刪掉。)

每一筆的格式(檔案裡每一筆上方都已經標好對應的圖檔名跟內容描述):

```ts
// 17_image_81.png  —  A single red flower among green leaves
"vp-03": {
  title: "",          // ← 填了這個,這件作品的 hover 跟詳情頁就會一起開啟
  description: "",
  meta: [],
},
```

### meta 欄位依分類不同(呼應待辦 6 第 2 點)

`meta` 是自由的「標籤 / 數值」清單,不是固定欄位,所以每個分類可以放不一樣的東西:

- **Visual Product 裡的攝影**:沿用設計稿的 EXIF 四欄

  ```ts
  meta: [
    { label: "Camera model", value: "EOS R100" },
    { label: "ISO speed", value: "ISO-500" },
    { label: "F-stop", value: "f/6.3" },
    { label: "Exposure time", value: "1/400 sec." },
  ],
  ```

- **Visual Product 裡的 3D 渲染、以及 Poster Design / Studio Drawing / 2D & 3D Animation**:EXIF 不適用,換成例如使用的軟體(Photoshop / Blender / Maya…)、媒材(鉛筆 / 炭筆 / 水彩)、是課堂作業還是自主創作。

### 一個已經處理掉的特例

`vp-08` 跟 `vp-11` 是**同一件 3D 作品**(分層藍色圓盤上的冰山),Figma 把它放了兩次、各自框住不同的圓盤。程式已經用 `sameAs` 把它們合併成一件:**文案只要寫在 `"vp-08"` 底下**,兩個圖塊都會吃到,而且共用同一個網址。所以總數是 49 個圖塊 / **48 件作品**。

---

## 待辦 9:CV 檔案與社群連結網址

**狀態:✅ 已完成(2026-08-18)。兩個都填進 `lib/site.ts` 了。**

1. **CV PDF ✅**:檔案是 `public/assets/cv/chai-gai-foon-cv.pdf`(812 KB),`cvPath` 已指向它。Resume 頁的「DOWNLOAD CV」按鈕現在是真的下載鈕(`<a download>`),點了會存檔而不是在瀏覽器裡開起來。
   > 原本的說明:沒填 `cvPath` 的時候按鈕長得跟設計稿一樣,但被刻意做成**點了沒反應**——這比放一個會 404 的連結好。之後換新版 CV,只要用同一個檔名覆蓋過去就行,不用動程式。
2. **社群連結 ✅**:三個都填好了,footer 的清單跟 Contact 頁中間那排圓形 icon 現在都可以點,一律新分頁開啟(`target="_blank"` + `rel="noreferrer noopener"`)。

   | | 網址 |
   |---|---|
   | Facebook | `https://www.facebook.com/gaifoon.chai` |
   | Instagram | `https://www.instagram.com/gaifoonchai/` |
   | Youtube | `https://www.youtube.com/@gaifoonchai424` |

   > 原本的說明:空字串的時候 footer 的社群清單會渲染成**純文字**(不是連結),Contact 頁的 icon 也點不動。這個 fallback 還留著,所以哪天想暫時下架某一個,把它的 `url` 清空即可,不用改版面。

三個帳號都存在、也都是公開的(屋主自己確認過,2026-08-18)。

---

## 待辦 10:詳情頁四個 frame 的高度 —— 要不要改成剛好填滿螢幕

**狀態:✅ 已完成(2026-08-18)。屋主選了「改」,Figma 三個 frame 都改高並重排完畢,程式端已同步。做法見本節末尾的「最後怎麼做的」。**

### 現況

`Artwork inside show` 的四個 frame(390 / 768 / 1440 / 1920)**高度都是 790**。程式會把整個構圖等比縮放去貼合視窗,寬高哪個先用完就用哪個當基準,所以任何螢幕都不會出現捲軸、也不會被裁掉。

但「不出現捲軸」跟「剛好填滿」是兩件事。只有畫面比例正好等於 frame 比例時才會完全填滿,實測:

| 視窗 | 構圖尺寸 | 結果 |
|---|---|---|
| 390×740 | 365×740 | 左右各留 12px |
| 768×950 | 768×790 | 上下各留 80px |
| **1440×790** | **1440×790** | **完全吻合** |
| 1920×940 | 1920×790 | 上下各留 75px |

1440 那張抓得很準——1440×900 的筆電在 Chrome 裡可用高度正好就是 790。

### 如果要每個都剛好填滿

把另外三個 frame 的高度改成各自裝置的實際可用高度:

| 設計寬 | 代表裝置 | 裝置解析度 | 瀏覽器實際可用高 | 建議 frame 高 |
|---|---|---|---|---|
| 390 | iPhone 12–14 | 390×844 | 初始 ~664,捲動後工具列收合 ~745 | **740** |
| 768 | iPad 直式 | 768×1024 | ~954 | **950** |
| 1440 | MacBook 14"/16" | 1440×900 | ~790 | **790**(維持) |
| 1920 | 桌機 1080p | 1920×1080 | ~945 | **940** |

改完 code 端只要動 `app/artwork/[slug]/page.module.css` 裡各斷點的 `--frame-h`,一個數字。

> 順帶一提:390 要特別注意 Safari 的工具列會隨捲動收合,可用高度在 664 和 745 之間跳。**重要內容放在前 660px 內**,一進頁面就看得到。程式用的是 `100dvh` 而不是 `100vh`,就是為了跟著這個收合走。

### 最後怎麼做的

屋主把三個 frame 改成建議高度並**把裡面的元素重新排過**,程式端用 Figma MCP 重新讀了四個 frame 的座標同步過來。

**原本我說「code 端只要動 `--frame-h`,一個數字」是錯的。** frame 變高之後裡面每個元素的 y 座標都跟著動了,只改高度的話元素會停在舊位置、下面空一大塊。實際改的是每個斷點的 `--frame-h` 加五個 y 值:

| | frame 高 | `--fig-y` | `--title-y` | `--desc-y` | `--meta-y` | `--arrow-y` |
|---|---|---|---|---|---|---|
| 390 | 790 → **740** | 235 → **191** | 459 → **402** | 513 → **456** | 657 → **600** | 313 → **269** |
| 768 | 790 → **950** | 244 → **256** | 562 → **584** | 622 → **644** | 724 → **746** | 375 → **387** |
| 1440 | **790**(不變) | 97 | 577 | 635 | 741 | 366 |
| 1920 | 790 → **940** | 97 → **130** | 577 → **627** | 635 → **685** | 741 → **791** | 366 → **441** |

**沒有變的**:所有寬度、x 座標、裝飾圓圈的位置與大小、關閉鈕、字級字距。大圖在四個斷點都是水平置中,程式本來就用 `left:50%` + `translateX(-50%)` 處理,所以 x 完全不用管。

**一個讀 Figma 時要注意的坑**:左右箭頭那兩個 Vector,`get_metadata` 回報的 y **比實際視覺位置多了一個箭頭高度**(它們是鏡射過的圖形)。四個 frame 都一致偏移:1440 回報 424、實際 366;1920 回報 499、實際 441;768 回報 427、實際 387;390 回報 299、實際 269。**1440 這個 frame 沒被改過,它的實際值 366 跟程式裡原本就有的數字完全吻合**,這個偏移規則才被確認下來。以後再讀這幾個節點要記得扣掉。

**驗證**:在 1536×791 的視窗實測,舞台是 1442×791 —— **高度完全填滿、沒有捲軸**,大圖 top 落在 97、標題落在 577,跟 Figma 座標一模一樣。1440×900 筆電在 Chrome 的可用高度實測正好 791,跟當初推估的 790 只差 1px。

**沒有驗證到的部分**:這台機器的瀏覽器視窗縮不到 768 以下(縮放設定擋住了),所以 390 / 768 / 1920 三個斷點**只有數字是從 Figma 抓來的,沒有真的用那個尺寸的視窗看過**。四個斷點走的是同一套機制、1440 已驗證無誤,理論上不會有問題,但你手邊有手機/平板的話開來確認一下比較保險。

---

## 待辦 11:幾個跨斷點的設計不一致 —— 等屋主拍板

**狀態:✅ 已結案(2026-08-18)。屋主決定「照目前做的就好」,四項全部維持下表「目前的做法」那一欄,程式不用改。**

| # | 狀況 | 目前的做法 | 備註 |
|---|---|---|---|
| 1 | **contact 768 的 footer 有社群連結,但 contact 1440 / 1920 沒有** | 照 1440 / 1920,**拿掉** | 理由是 Contact 頁中間本來就有一排大顆的社群 icon,footer 再放一次會重複——這個理由在 768 一樣成立 |
| 2 | **Resume 390 的技能清單分兩欄**,你的稿是 My Skills 分 5 / 6 | 用 CSS 自動分欄,會**平均成 6 / 5** | 差一項。介意的話可以改成寫死的兩份清單 |
| 3 | **Poster Design 的 intro 在 390 是靠左,其他三個斷點是置中** | **照做**,分斷點存 | 這個看起來是刻意的,不是手滑 |
| 4 | **Resume 第一段學歷敘述的字距是 2.7,第二段是 1.8** | **照做** | 之前確認過不用統一 |

---

## 待辦 12:網址改成有意義的 slug

**狀態:✅ 已完成(2026-08-18)。48 個詳情頁全部換成從標題產生的可讀網址。因為網站當時還沒部署過,沒有任何舊連結會失效,所以不需要做 redirect。**

### 換之前 / 換之後

```
/artwork/vp-03   →   /artwork/one-red-flower
/artwork/sd-05   →   /artwork/portrait-of-tesla
/artwork/pd-05   →   /artwork/water-bankruptcy
/artwork/the-duck    (唯一沒變的,它本來就是從標題來的)
```

### 關鍵決定:`id` 與 `slug` 分家

原本 `WORKS` 的 `id` 跟 `slug` 是同一組值(都是 `vp-01`),而 `lib/artwork-content.ts` 是**用 slug 當 key**。如果直接把 slug 換掉,文案檔的 key 也得跟著換,連帶這兩份文件裡所有 `vp-08`、`sd-05` 之類的引用全部失效。

所以改成**兩者分工**:

| 欄位 | 用途 | 值 | 會不會變 |
|---|---|---|---|
| `id` | 內部識別;文案檔的 key、React key、文件裡的引用 | `vp-01`、`sd-05`… | **不會** |
| `slug` | 只負責公開網址 | `yellow-ixora`… | 改標題才會想動 |

因此:

- **`lib/artwork-content.ts` 的 key 一個都沒動**,還是 `vp-01`、`the-duck` 這些;這兩份文件裡的所有引用也全部繼續有效。
- 唯一被迫調整的是 The Duck:它的 `id` 本來是 `vp-the-duck`,跟文案 key `the-duck` 對不上,已改成 `the-duck` 讓兩者一致。
- 三個檔案的查找改成用 `id`:`app/artwork/page.tsx`、`app/preview/artwork/page.tsx`、`app/artwork/[slug]/page.tsx`。連結與 `generateStaticParams` 則繼續用 `slug`。
- `sameAs` 存的是 `id`(值本來就是 `vp-08`,不用改)。列表頁遇到 `sameAs` 的圖塊時會先找出擁有者,再拿它的 `slug` 當連結——所以那兩個共用圖塊都指向 `/artwork/freshwater-shelf`。
- `vp-11` 是第二次裁切、永遠不會有自己的網址,它的 `slug` 就留著沒動。

### 驗證

- 48 個新網址**全部回 200**;舊的 `/artwork/vp-01` 回 404(預期,因為從沒公開過)。
- 上一件 / 下一件的環狀鏈:**48 件、無重複、繞得回起點、prev 與 next 對稱**。
- 列表頁 49 個圖塊 → **48 個不重複網址**,共用裁切的那兩塊都指向同一個。
- `/preview/artwork` 顯示的仍然是文案檔的 key(`vp-01`),不是網址,所以它照樣能拿來對照。

### 以後改標題會怎樣

**不會自動改網址。** slug 是寫死在 `lib/artwork.ts` 裡的資料,不是從標題即時算出來的——這是刻意的,否則改一個錯字就會把已經分享出去的連結弄壞。想同步的話要自己去改 `slug`,而且**上線之後再改就得補 redirect**(`next.config.ts` 的 `redirects()`)。

---

## 待辦 13:素材與 Figma 檔案的整理

**狀態:✅ 三項都結案(2026-08-18)。第 1 項已壓縮,第 2、3 項屋主決定不動。**

1. **`46_poster_fyp_2.png` ✅ 已壓縮**:10MB → **3.46MB**,尺寸維持 3368×4760 沒變,現在只比第二大的 `39_image_119.png`(2.63MB)大一點,不再是「第二名的 4 倍」。開發時的載入等待已經改善。
   - 壓縮方式是**轉成 256 色索引色盤**(PNG colorType 3)。用 1:1 放大檢查,暗部漸層跟平滑曲面看得到色階斷層(banding)——但網站上這張最大只顯示到 419px 寬(1920 斷點),等於從 3368px 縮小 8 倍,縮圖過程會把斷層平均掉,實際頁面上看不出來。**維持現狀沒問題。**
   - ⚠️ **原始的 10MB 檔案已經被覆蓋掉,救不回來**(壓縮發生在建 git repo 之前)。未壓縮的原稿(或 PS/AI 來源檔)請自己在專案外另外留一份——哪天要拿去印刷、投稿或放大展示,現在這個 256 色的版本是不夠的。
   - 好消息是**同樣的事不會再發生**:專案已經在 2026-08-18 建了 git repo,之後任何檔案被蓋掉都能還原。
   - 順帶一提,訪客本來就拿不到這個 3.46MB 的原檔:頁面走 `next/image`,實際送出去的是 640px 的 webp,**84KB**。所以壓縮主要是改善開發體驗跟專案體積,不是訪客的載入速度。
2. **Figma canvas 上散落在 frame 外的節點**(`473:1531「768 saiz」`、`473:1528 image 78`、Artwork 1920 裡的 `Group`)——**屋主決定不理它們,留著**。程式完全沒有用到,留著也不影響網站。
3. **`public/assets/_unused/`** ——**屋主決定先存著**。裡面是被取代掉的匯出檔(未裁切的 `image 116/117` 原圖、跟 `47_image_96.png` 重複的 `48_image_106.png` 等),共約 3.2MB。網站上沒有任何地方連到它們,所以訪客不會載到,對載入速度沒有影響。
   - 只有一點要知道:`public/` 底下的東西**會原封不動跟著部署上去**,而且**知道網址的人可以直接開**(實測 `/assets/_unused/48_image_106_duplicate_of_47.png` 回 200)。裡面目前都只是你自己的作品匯出檔,沒有私密內容,所以不要緊;但以後**不要把不想被看到的東西放進 `public/`**——那個資料夾等於公開空間,`_unused` 這個名字不會讓它變成私人的。

---

## 待辦 14:Artwork 列表頁 hover 疊層的字級

**狀態:✅ 已完成(2026-08-18)。屋主反映「懸停後字太小、而且每塊大小不一」,已改成上下限收窄。**

### 問題

疊層字級原本是純 `cqw`(跟著圖塊寬度等比縮放)。當初只有 The Duck 一件有文案,那塊是 344×364,照著它訂規格沒問題。48 件全部寫完之後,49 個圖塊在 1440 斷點的寬度從 **187px 到 725px**,於是:

| | 原本 | 現在 |
|---|---|---|
| 敘述字級 | 6.5px ~ 25.3px(**差 3.9 倍**) | 11px ~ 15px(**差 1.36 倍**) |
| 標題字級 | 12px ~ 46px | 16px ~ 27px |

6.5px 根本看不清楚,25px 又太吵,而且每一塊都不一樣大。

### 做法

`clamp()` 把 `cqw` 夾在上下限之間,**49 塊裡有 31 塊剛好落在上限或下限**,所以看起來幾乎是統一的字級,又不必犧牲小圖。

- 上下限用 `rem` 不用 `px`,才會跟著各斷點的 root font-size 走。
- 字距改用 `em`(標題 `0.136em`、敘述 `0.08em`),會跟著夾過的字級走,不會再照原始圖塊寬度亂飄。
- **標題下限是 16px 不是 20px**:最窄的圖塊文字框只有 159px,而所有標題裡最長的不可斷字是 `Oppenheimer,` —— 20px 時量到 183px,會被圖塊的 `overflow: hidden` 裁掉半個字,看起來像壞掉;16px 是 147px,安全。另外加了 `overflow-wrap: break-word` 當保險。
- `.work` 的 `container-type` 從 `inline-size` 改成 **`size`**,這樣疊層也能對「圖塊多高」反應(每個圖塊都有明確的 w/h,所以 size containment 沒有副作用)。矮於 240px 的圖塊行高收成 1.5,其餘維持 1.8。
- 敘述加了 `min-height: 0; overflow: hidden`。疊層是靠下對齊的,沒有這行的話,文字太多時會把**標題頂出圖塊外面**,整段變成沒有標題的殘句。現在改成敘述自己截掉尾巴,標題一定看得到。

### 結果

49 塊全部:標題不被裁、標題不被頂出去。只有 3 塊(`Bougainvillea` 187×165、`Tapir` 187×162、`Antarctic Adventure` 372×210)的敘述尾端被切掉 **4~8px** —— 不到半行,完整內容在詳情頁看得到。

### 之後要注意

疊層的空間是圖塊給的,不是無限的。**敘述寫太長,小圖塊就會從尾端被切掉**(詳情頁不受影響,那邊的限制見待辦 8)。目前最吃緊的是那三塊 187~372px 寬、160~210px 高的圖。

---

## 待辦 15:詳情頁的上一件 / 下一件——系列作品要連在一起

**狀態:✅ 已完成(2026-08-18)。屋主反映「Portrait of Tesla 系列與 Still Life Sketch 系列在詳情頁混在一起沒有連貫」。**

### 問題

詳情頁的左右箭頭是照 `lib/artwork.ts` 裡 `WORKS` 的順序走,而 `WORKS` 是**版面順序**(照畫布由上而下、由左而右排)。Studio Drawing 這一區的設計把兩個系列**交錯**擺在畫布上:

| slug | y(1440) | 標題 |
|---|---|---|
| sd-04 | 6006 | Still Life Sketch 1 |
| sd-05 | 6121 | Portrait of Tesla |
| sd-06 | 6268 | Still Life Sketch 2 |
| sd-07 | 6544 | Portrait of Tesla 2 |
| sd-08 | 6544 | Still Life Sketch 3 |
| sd-09 | 7009 | Still Life Sketch 4 |
| sd-10 | 7068 | Portrait of Tesla 3 |

所以按下一件會變成「素描 1 → 特斯拉 → 素描 2 → 特斯拉 2 →……」,兩個系列都讀不成一組。

### 為什麼不直接改 `WORKS` 的順序

改了確實能解決,而且**實測 49 個圖塊在四個斷點都沒有任何重疊**,所以調動陣列順序不會影響版面外觀。但 `WORKS` 的順序目前**剛好等於視覺閱讀順序**(已驗證,排序後完全一致),那正是鍵盤 Tab 的移動順序。為了詳情頁去打亂列表頁的 Tab 順序不划算。

### 做法

改成**列表頁維持版面順序,詳情頁自己算一套順序**。

1. `lib/artwork-content.ts` 的 `WorkContent` 加一個選填欄位 `series`,字串相同就是同一系列。目前標了 7 筆:`sd-04/06/08/09` = `"Still Life Sketch"`,`sd-05/07/10` = `"Portrait of Tesla"`。
2. `app/artwork/[slug]/page.tsx` 的 `published()` 在過濾之後多一步:**把整個系列當成一塊,擺在該系列第一件出現的位置**,其餘作品位置不動。

### 結果

```
Afternoon Tea
Still Life Sketch 1 → 2 → 3 → 4
Portrait of Tesla → 2 → 3
Oppenheimer Poster
```

環狀鏈驗證:**48 件、無重複、可繞回起點、prev 與 next 完全對稱**。

### 以後要加系列時

在該作品的 `series` 填同一個字串就會自動連在一起,不用動 `WORKS`,也不用管它在畫布上排在哪裡。

---

## 待辦 16:(之後有其他要留到 VS Code 做的項目,往下加)
